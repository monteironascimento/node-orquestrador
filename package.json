{
  "name": "node-lomadee",
  "version": "1.0.0",
  "description": "Sincronizacao de Ofertas",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=developement ts-node-dev --inspect --transpile-only --ignore-watch node_modules --respawn src/server.ts",
    "test": "NODE_ENV=test ts-node src/server.ts",
    "start": "NODE_ENV=production ts-node src/server.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^8.0.29",
    "@types/shelljs": "^0.8.8",
    "nodemon": "^2.0.7",
    "ts-node-dev": "^1.1.6"
  },
  "dependencies": {
    "@types/request": "^2.48.5",
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "express-timeout-handler": "^2.2.0",
    "fs": "^0.0.1-security",
    "jimp": "^0.16.1",
    "reflect-metadata": "^0.1.13",
    "request": "^2.88.2",
    "shelljs": "^0.8.4",
    "ts-node": "^9.1.1",
    "typescript": "3.3.3333"
  },
  "nodemonConfig": {
    "ignore": [
      "tokens/*",
      "docs/*"
    ],
    "delay": "2500"
  }
}
